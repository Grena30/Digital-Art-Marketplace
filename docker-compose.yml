services:
  login-service:
    build: ./login-service
    ports:
      - "5000:5000"
    depends_on:
      - login-database
    networks:
      - marketplace-network

  login-database:
    build: ./login-database
    environment:
      POSTGRES_DB: logindb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - login-db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - marketplace-network

networks:
  marketplace-network:
    driver: bridge

volumes:
  login-db-data: